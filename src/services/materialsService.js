js

import { db, ref, get, set, update, push } from "../firebase.js";  export function materialItemPath(pid, itemId){ return projects/${pid}/materials/catalog/items/${itemId}; }  export async function listCatalog(pid){   const snap = await get(ref(db, projects/${pid}/materials/catalog/items));   const obj = snap.exists() ? snap.val() : {};   *// normalize*    return Object.entries(obj).map(([itemId, it]) => {     return {       itemId,       itemName: it?.itemName || "",       category: it?.category || "",       unitDefault: it?.unitDefault || "",       variants: it?.variants || {}     };   }); }  export async function upsertMaterialItem(pid, { itemId, itemName, category, unitDefault, variants }){   if(!itemId){     const newRef = push(ref(db, projects/${pid}/materials/catalog/items));     itemId = newRef.key;   }   const payload = { itemName, category, unitDefault: unitDefault || "", variants: variants || {} };   await set(ref(db, projects/${pid}/materials/catalog/items/${itemId}), payload);   return itemId; }  export async function deleteMaterialItem(pid, itemId){   await set(ref(db, projects/${pid}/materials/catalog/items/${itemId}), null); }  export async function getStock(pid){   const snap = await get(ref(db, projects/${pid}/materials/stock));   return snap.exists() ? snap.val() : {}; }  export async function upsertStock(pid, itemId, variantId, { quantity, unit, volume }){   await update(ref(db, projects/${pid}/materials/stock/${itemId}/${variantId}), {     quantity: Number(quantity || 0),     unit: unit || "",     volume: volume == null ? null : Number(volume),     updatedAt: new Date().toISOString(),   }); }
